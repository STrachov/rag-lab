import { PlaceholderPage } from "./PlaceholderPage";

export function SavedExperimentsPage() {
  return (
    <PlaceholderPage
      eyebrow="Experiments"
      title="Saved Experiments"
      body="Saved experiments will connect data, full parameter snapshots, optional ground truth, and metrics-only results."
    />
  );
}
