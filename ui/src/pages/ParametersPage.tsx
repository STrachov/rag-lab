import { PlaceholderPage } from "./PlaceholderPage";

export function ParametersPage() {
  return (
    <PlaceholderPage
      eyebrow="Parameters"
      title="Parameter Sets"
      body="Preparation, chunking, indexing, retrieval, reranking, generation, and evaluation parameters will be managed here."
    />
  );
}
