import { PlaceholderPage } from "./PlaceholderPage";

export function SettingsPage() {
  return (
    <PlaceholderPage
      eyebrow="Settings"
      title="Settings"
      body="Project defaults and runtime adapter settings will be configured here later."
    />
  );
}
